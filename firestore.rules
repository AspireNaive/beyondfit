rules_version = '2';

// The Node API (Admin SDK, which bypasses rules) is the only client of this
// database. Browsers never talk to Firestore directly, so everything is closed.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
