# Fail the install outright on an unsupported Node rather than building a tree
# with wrong-ABI native binaries. Without this, `npm i` on Node 16 "succeeds"
# and then Vite dies with a cryptic `styleText` SyntaxError at run time.
engine-strict=true
